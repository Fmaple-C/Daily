# 枫叶计划项目技术文档

## 技术目标

本项目当前目标是做一个本地离线、可持续迭代的 Android 计划应用。实现上优先保持依赖少、结构清楚、数据可迁移。

技术原则：

- 数据优先保存在本机。
- 第一阶段不引入第三方依赖。
- 业务规则、数据读写和界面代码分层。
- 每次数据结构升级必须有 `schemaVersion` 和迁移策略。
- 构建产物为 Android APK。

## 当前技术栈

| 类别 | 当前选择 |
| --- | --- |
| 平台 | Android |
| 语言 | Java |
| UI | 原生 Android View，以代码方式构建界面 |
| 本地存储 | `SharedPreferences` + 带版本号的 JSON |
| 构建工具 | Gradle Wrapper |
| Android Gradle Plugin | `8.3.2` |
| Compile SDK | `34` |
| Min SDK | `23` |
| 应用包名 | `com.maple.daily` |

## 项目结构

```text
Daily/
├── app/
│   ├── build.gradle
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/maple/daily/
│       │   ├── MainActivity.java
│       │   ├── data/
│       │   │   ├── PlanRepository.java
│       │   │   └── ThemePreferences.java
│       │   ├── domain/
│       │   │   └── PlanRules.java
│       │   ├── model/
│       │   │   ├── CheckIn.java
│       │   │   ├── PlanConstants.java
│       │   │   └── PlanItem.java
│       │   └── util/
│       │       └── DateKeys.java
│       ├── res/drawable/
│       │   └── ic_launcher_foreground.xml
│       ├── res/mipmap-anydpi/
│       │   ├── ic_launcher.xml
│       │   └── ic_launcher_round.xml
│       ├── res/mipmap-anydpi-v26/
│       │   ├── ic_launcher.xml
│       │   └── ic_launcher_round.xml
│       └── res/values/
│           ├── colors.xml
│           ├── strings.xml
│           └── styles.xml
├── docs/
│   ├── 功能文档.md
│   └── 技术文档.md
├── build.gradle
├── gradle.properties
├── settings.gradle
├── gradlew
└── gradlew.bat
```

## 架构分层

### `MainActivity`

负责界面和用户交互。

主要职责：

- 构建顶部紧凑导航、右上角新建入口、主题切换入口、创建表单、计划列表和完整打卡历史页。
- 响应新建计划、选择日期、选择时间、打卡、删除等操作。
- 调用 `PlanRepository` 读取和保存数据。
- 调用 `PlanRules` 判断计划状态。
- 使用 `DateKeys` 生成日期和时间展示。

不负责：

- 直接读写 `SharedPreferences`。
- 直接解析或生成 JSON。
- 直接处理数据版本迁移。

### `model`

保存数据结构和常量。

- `PlanItem`：计划数据模型。
- `CheckIn`：单次打卡记录。
- `PlanConstants`：计划类型、周期模式常量。

### `data`

负责本地数据读写。

- `PlanRepository`：封装 `SharedPreferences`、JSON 解析、JSON 保存和旧数据迁移。
- `ThemePreferences`：保存日夜主题偏好。

### `domain`

负责业务规则。

- `PlanRules`：判断计划是否在当前日期生效、是否在当前周期已打卡、非生效状态文案。

### `util`

负责通用工具。

- `DateKeys`：生成 `yyyy-MM-dd`、`yyyy-MM`、`yyyy`、月初、月末、年初、年末、时间格式化和日期解析。

## 数据模型

### `PlanItem`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `id` | `String` | 计划唯一标识，当前使用创建时毫秒时间戳 |
| `type` | `String` | 计划类型：`daily`、`monthly` 或 `yearly` |
| `title` | `String` | 计划名称 |
| `note` | `String` | 备注，多行文本 |
| `scheduleMode` | `String` | 周期模式 |
| `startDate` | `String` | 开始日期，格式 `yyyy-MM-dd`，部分模式为空 |
| `endDate` | `String` | 结束日期，格式 `yyyy-MM-dd`，部分模式为空 |
| `deadlineMinutes` | `int` | 日计划截止时间，按当天 0 点起算的分钟数 |
| `createdAt` | `long` | 创建时间戳，单位毫秒 |
| `checkIns` | `List<CheckIn>` | 打卡记录 |

### `CheckIn`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `id` | `String` | 打卡记录唯一标识 |
| `targetType` | `String` | 打卡所属类型：`daily`、`monthly` 或 `yearly` |
| `targetKey` | `String` | 打卡周期键，日计划为 `yyyy-MM-dd`，月计划为 `yyyy-MM`，年计划为 `yyyy` |
| `checkedAt` | `String` | 打卡展示时间，例如 `2026-05-14 15:30` |
| `note` | `String` | 本次打卡备注 |
| `checkedAtMillis` | `long` | 打卡时间戳，单位毫秒 |

## 周期模式

| 常量 | 含义 |
| --- | --- |
| `daily_every` | 每日生效 |
| `daily_today` | 只在创建当日生效 |
| `monthly_every` | 每月生效 |
| `monthly_this` | 只在当前月份生效 |
| `yearly_every` | 每年生效 |
| `yearly_this` | 只在当前年份生效 |
| `range` | 指定开始日期到结束日期之间生效 |

## 本地存储方案

当前使用 `SharedPreferences`：

- 文件名：`daily_maple_prefs`
- 当前数据键：`daily_data`
- 旧版兼容键：`todos`
- 当前数据版本：`schemaVersion = 3`
- 当前数据格式：包含 `schemaVersion` 和 `plans` 的 JSON 对象字符串

示例 JSON：

```json
{
  "schemaVersion": 3,
  "plans": [
    {
      "id": "1778730000000",
      "type": "daily",
      "title": "每日记账",
      "note": "1. 早餐-4元",
      "scheduleMode": "daily_every",
      "startDate": "",
      "endDate": "",
      "deadlineMinutes": 720,
      "createdAt": 1778730000000,
      "checkIns": [
        {
          "id": "1778730000000-1778733600000",
          "targetType": "daily",
          "targetKey": "2026-05-14",
          "checkedAt": "2026-05-14 15:30",
          "note": "今天支出记录完成",
          "checkedAtMillis": 1778733600000
        }
      ]
    }
  ]
}
```

## 数据迁移规则

### `schemaVersion = 1`

旧格式：

```json
{
  "schemaVersion": 1,
  "todos": []
}
```

迁移到当前版本：

- 每个旧 TODO 转为一个 `daily` 日计划。
- 周期模式设为 `daily_every`。
- 旧 `deadlineMinutes` 保留。
- 旧 `lastCompletedDate` 如果存在，会生成一条迁移打卡记录。
- 旧版本没有保存具体打卡时间，所以迁移记录使用 `当天 00:00`。
- 当前代码会保存为最新的 `schemaVersion = 3`。

### 旧版无版本数据

更早版本可能只保存 `todos` 数组。读取时会自动转为当前最新数据结构。

### `schemaVersion = 2`

旧格式已经包含 `plans` 和 `checkIns`，但 `CheckIn` 没有 `note` 字段。

迁移到 `schemaVersion = 3`：

- 读取旧打卡记录时，`note` 使用空字符串。
- 保存时写回 `schemaVersion = 3`。

### 后续规则

- 每次新增、删除或改变持久化字段，都要评估是否提升 `schemaVersion`。
- 新版本必须兼容旧版本数据。
- 迁移成功后应保存为最新结构。
- 不建议覆盖未知的更高版本数据，避免降级安装破坏未来数据。

## 日期与状态逻辑

## 主题方案

当前日夜主题由 `MainActivity` 内的调色板控制，用户选择由 `ThemePreferences` 保存。

当前规则：

- `false`：日间主题。
- `true`：夜间主题。
- 切换主题后重建当前界面。
- 主题偏好与计划数据分开保存，避免业务数据和 UI 偏好混在一起。

## 应用图标

应用图标使用 Android 原生资源：

- `ic_launcher_foreground.xml`：枫叶和对勾矢量前景。
- `mipmap-anydpi-v26/ic_launcher.xml`：adaptive icon。
- `mipmap-anydpi/ic_launcher.xml`：旧版本 fallback。

## 日期与状态逻辑

### 生效判断

`PlanRules.isActiveOnDate(plan, dateKey)` 负责判断计划是否在指定日期生效。

规则：

- `daily_every`、`monthly_every` 和 `yearly_every` 永远生效。
- `daily_today`、`monthly_this`、`yearly_this` 和 `range` 按 `startDate <= today <= endDate` 判断。

### 打卡判断

当前周期是否打卡由 `targetKey` 判断。

- 日计划当前周期键：`yyyy-MM-dd`。
- 月计划当前周期键：`yyyy-MM`。
- 年计划当前周期键：`yyyy`。

只要当前周期内存在至少一条打卡记录，就认为当前周期已打卡。

### 超时判断

当前只有日计划有超时状态。

规则：

```text
如果计划今天生效
并且今天没有打卡
并且当前分钟数 > deadlineMinutes
则显示已超时
```

## 构建方式

在项目根目录执行：

```powershell
.\gradlew.bat assembleDebug
```

生成 APK：

```text
app\build\outputs\apk\debug\Daily-版本-日期.apk
```

同时会保留一份到项目根目录的本地归档目录：

```text
release-apks\Daily-版本-日期.apk
```

当前构建命名规则由 `app/build.gradle` 自动控制：

```text
Daily-${versionName}-${yyyyMMdd}.apk
```

例如：

```text
Daily-0.5.1-20260514.apk
```

版本维护规则：

- 每次发布功能更新时，提升 `versionName`。
- 每次准备安装到已有手机应用上时，提升 `versionCode`，保持单调递增。
- 不手动重命名 APK，避免文件名和应用版本不一致。
- 构建新版时不执行清理旧包操作；通过新的 `versionName` 生成不同 APK 文件名，并将 APK 追加保留到 `release-apks`。

如果 Gradle 全局缓存损坏，可以尝试：

```powershell
.\gradlew.bat --stop
```

然后删除用户目录下的 Gradle 可再生缓存，例如：

```text
%USERPROFILE%\.gradle\caches\transforms-4
```

删除前需要确认路径确实位于 `.gradle\caches` 内。

## 当前限制

- `MainActivity` 仍然承担较多 UI 构建工作，后续可以继续拆成独立 UI 组件或迁移到 Compose。
- 当前完整打卡历史页按单个计划展示全部记录；后续可继续扩展为全局历史、筛选和搜索。
- 当前仍使用 `SharedPreferences`，大量历史数据后建议迁移到 SQLite 或 Room。
- 没有通知提醒。
- 没有自动化测试。
- 没有适配横屏、大屏和平板布局。

## 后续技术演进建议

### 阶段 1：继续整理 UI

- 把创建表单拆成 `PlanFormView`。
- 把计划卡片拆成 `PlanCardView`。
- 把顶部导航拆成 `PlanNavigationView`。
- 当前新建表单已默认收起，后续拆分 UI 时保留“列表优先”的布局原则。

### 阶段 2：补齐核心体验

- 编辑计划名称和备注。
- 增加全局打卡历史、筛选和搜索。
- 增加提醒通知。
- 增加数据导入导出。

### 阶段 3：升级存储

当打卡历史变多后，建议迁移到 SQLite 或 Room。

建议表结构：

- `plans`：保存计划基础信息。
- `check_ins`：保存每次打卡记录。

这样可以支持：

- 连续打卡天数。
- 月历视图。
- 每月完成率。
- 按计划查询历史。

### 阶段 4：测试

建议补充：

- `PlanRules` 单元测试。
- `DateKeys` 日期边界测试。
- `PlanRepository` 迁移测试。
- UI 冒烟测试。

重点测试边界：

- `00:00` 跨日。
- 刚好等于截止时间。
- 超过截止时间后补打卡。
- 日期范围开始和结束当天。
- 月计划跨月范围。
- 年计划跨年范围。

## 文档维护规则

后续修改技术方案时，需要同步更新本文档。尤其是以下变化：

- 存储方案变化。
- 数据模型字段变化。
- 架构拆分。
- 构建环境变化。
- 新增第三方依赖。
- 重要边界逻辑变化。
